describe('event', function() {
    /*
    it('simple on and trigger', function() {
        var $ev = $('#event')
        var i = 0
        var handler = function() {
            i++
        }
        $ev.on('click', handler).trigger('click')
        assert.equal(i, 1)
    })
    */
})
